{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug Odin (LLDB)",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/game.exe",
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "make: debug",
      "stopOnEntry": false
    }
  ]
}