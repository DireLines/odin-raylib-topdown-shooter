{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug Odin (LLDB)",
      "type": "lldb-dap",
      "request": "launch",
      "program": "${workspaceFolder}/game",
      "cwd": "${workspaceFolder}",
      "args": []
    }
  ]
}
